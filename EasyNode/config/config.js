// 单数的配置文件

// 服务器端口
const port = 8008

// 数据库配置
const mysqlConfig = {
  host: 'localhost',
  port: 3306,
  database: 'easy_blog',
  user: 'root',
  password: 'Gray@1943'
}

module.exports = { port, mysqlConfig }
